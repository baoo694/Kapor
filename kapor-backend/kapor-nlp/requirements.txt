fastapi==0.109.2
uvicorn==0.27.1
pydantic==2.6.1
konlpy==0.6.0
python-mecab-ko==1.3.3
# WhisperX provides the Korean forced-alignment timestamps used by the mobile timeline.
# Pin it so a model/runtime change is reviewed against our timing fixtures.
whisperx==3.4.2
# WhisperX 3.4.2 supports CTranslate2 < 4.5. 4.4 is compatible with the
# CUDA 12 + cuDNN 8 GPU image used by Dockerfile.gpu.
ctranslate2==4.4.0
python-multipart==0.0.20
requests>=2.31,<3
