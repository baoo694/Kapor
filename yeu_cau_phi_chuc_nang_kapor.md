### 3.4. Yêu cầu phi chức năng

Yêu cầu phi chức năng mô tả các đặc tính kỹ thuật đã được hiện thực trong hệ thống Kapor về hiệu năng, bảo mật, độ tin cậy, khả năng mở rộng, khả năng bảo trì, khả năng sử dụng và triển khai. Bảng 3.3 tổng hợp các yêu cầu phi chức năng có thể truy vết trực tiếp đến cấu hình và mã nguồn hiện tại của dự án.

#### Bảng 3.3. Danh sách Yêu cầu phi chức năng (NFR)

| ID | Loại | Mô tả yêu cầu |
| :--- | :--- | :--- |
| **NFR-01** | Hiệu năng (Performance) | **Tái sử dụng dữ liệu âm thanh TTS:** Âm thanh do Gemini TTS tạo được lưu theo khóa phụ thuộc vào nội dung, chế độ đọc, model, giọng đọc và phiên bản prompt trong MinIO. Các yêu cầu trùng khớp đọc lại tệp đã lưu, giúp giảm số lần gọi Gemini và rút ngắn thời gian phản hồi cho nội dung đã được tổng hợp trước đó. |
| **NFR-02** | Hiệu năng (Performance) | **Kiểm soát lời gọi dịch vụ ngoại vi:** TechTalk giới hạn mặc định 5 lượt/phút, 100 lượt/ngày và 30 lượt/phiên bằng bộ đếm Redis, đồng thời có bộ đếm cục bộ khi Redis tạm thời không khả dụng. TTS giới hạn 12 lần tạo âm thanh mới mỗi phút cho mỗi người dùng. Các lời gọi AI và xử lý giọng nói đặt timeout theo nghiệp vụ: Gemini TTS 25 giây; Honorifics và Gemini Pronunciation 30 giây; Azure và TechTalk 45 giây; WhisperX và xử lý phụ đề 90 giây. |
| **NFR-03** | Bảo mật (Security) | **Xác thực an toàn:** Tài khoản email sử dụng mật khẩu được băm một chiều bằng BCrypt. Backend xác thực stateless bằng JWT, phân biệt access token và refresh token; access token có thời hạn mặc định 15 phút, refresh token 30 ngày và được xoay vòng sau mỗi lần làm mới. Luồng đặt lại mật khẩu sử dụng OTP có TTL, thời gian chờ gửi lại, hạn mức gửi và giới hạn số lần xác minh trên Redis. |
| **NFR-04** | Bảo mật (Security) | **Phân quyền truy cập (RBAC):** Các API `/api/admin/**` yêu cầu quyền `ROLE_ADMIN`; các API nghiệp vụ còn lại yêu cầu JWT hợp lệ. Yêu cầu thiếu hoặc có access token không hợp lệ nhận phản hồi 401, còn tài khoản đã xác thực nhưng thiếu quyền nhận phản hồi 403. Các luồng đăng ký, đăng nhập, làm mới token, quên mật khẩu và đặt lại mật khẩu sử dụng các endpoint công khai dưới `/api/auth`; workflow triển khai sử dụng endpoint công khai `/actuator/health` để kiểm tra trạng thái API. |
| **NFR-05** | Bảo mật (Security) | **Bảo vệ bí mật và dữ liệu ghi âm:** JWT secret, khóa Gemini, thông tin Azure, SMTP và MinIO được backend đọc từ biến môi trường khi chạy. File ghi âm được lưu trong MinIO và chỉ được trả qua API đã xác thực sau khi kiểm tra quyền sở hữu theo người dùng, phiên và mã bản ghi. Bản ghi Pronunciation và TechTalk có thời hạn lưu mặc định bảy ngày và được tác vụ định kỳ xóa khỏi kho đối tượng. |
| **NFR-06** | Khả dụng & Độ tin cậy (Reliability) | **Xử lý lỗi và cơ chế dự phòng:** `GlobalExceptionHandler` chuẩn hóa lỗi xác thực, phân quyền, kiểm tra dữ liệu, giới hạn tần suất và lỗi dịch vụ AI thành cấu trúc `ApiResponse` cùng mã HTTP phù hợp. TechTalk thử lại một lần đối với lỗi Gemini có thể phục hồi; Honorifics giữ kết quả từ bộ quy tắc khi Gemini không khả dụng; Pronunciation tạo nhận xét xác định từ dữ liệu Azure khi Gemini không tạo được phần giải thích. |
| **NFR-07** | Khả năng mở rộng (Scalability) | **Tách dịch vụ nhận dạng và căn chỉnh giọng nói:** WhisperX được triển khai trong dịch vụ FastAPI `kapor-nlp` để tạo transcript và mốc thời gian theo từ cho bản ghi tiếng Hàn của Pronunciation và transcript cho lượt nói TechTalk. Docker Compose chạy dịch vụ này trong container riêng bằng NVIDIA GPU (CUDA, mặc định 12 GB bộ nhớ container). Backend Spring Boot gọi dịch vụ qua `WHISPERX_SERVICE_URL` và bật Java 21 virtual threads để thực hiện các tác vụ đồng thời. |
| **NFR-08** | Khả năng bảo trì (Maintainability) | **Tổ chức mã nguồn và kiểm thử tự động:** Backend được chia theo domain và các lớp Controller – Service – Repository, sử dụng DTO, `ApiResponse` và bộ xử lý lỗi tập trung để thống nhất giao tiếp. Dự án có kiểm thử unit/integration cho các domain chính; workflow Backend CI chạy `mvn verify` và dựng Docker image trên các thay đổi backend được đẩy lên nhánh chính hoặc pull request. |
| **NFR-09** | Khả năng sử dụng (Usability) | **Trải nghiệm đa nền tảng:** Ứng dụng người học được xây dựng bằng Flutter với một codebase dùng chung cho Android và iOS. Điều hướng được quản lý bằng `go_router`, trạng thái giao diện bằng Provider; các luồng cần âm thanh sử dụng chung thành phần ghi âm, phát audio và xử lý quyền microphone trên ứng dụng. |
| **NFR-10** | Triển khai (Deployment) | **Đóng gói và triển khai bằng container:** Backend sử dụng Dockerfile nhiều giai đoạn với Java 21. Docker Compose khai báo Spring Boot API, dịch vụ FastAPI chạy WhisperX, MongoDB 7, Redis 7 và MinIO cùng các volume lưu dữ liệu; MongoDB và Redis có health check, API và WhisperX có giới hạn bộ nhớ, các dịch vụ có chính sách tự khởi động lại. Admin Panel được build bằng Node và phục vụ bằng Nginx trong container riêng. Workflow triển khai lấy đúng commit đã vượt qua Backend CI, cập nhật dịch vụ API trên VM và xác minh `/actuator/health` sau khi triển khai. |
